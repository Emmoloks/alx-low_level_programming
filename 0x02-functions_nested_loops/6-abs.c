#include "main.h"
/**
 * _abs - it calculates the absolute value of an integer
 * @i: The integer to calculate its absolute
 * Return: absolute value
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
